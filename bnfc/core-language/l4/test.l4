# Comments with sharp

class Business (1234) {
  is_legal (89): Bool
}

class Boo (345) extends Foo {
  boob (10): Bool -> Int
}


decl megacorp : Business

assert
# (\x : () -> x * x)  3
# megacorp.is_legal = True
# all x : Int. ex y : Int. x < y || not x = y && z = 5

# if x < 3 + 4 then x && z < 3 else if x = 4 then 5 else 6

      
# OK:
# \ (x, y) : (Int, Int) -> x < y + 3
# \x : Int -> \y : Int -> x < y + 3
# \x : Int -> \y : Int -> (f x).foo.zoo 

